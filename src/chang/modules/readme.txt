1、请勿 [[ [[]] ]]，应该这样：[[ [==[]==] ]]，IDEA 智能注释功能导致一不小心就嵌套了。。。

2、！！！不同模块之间的一个高内聚低耦合，避免循环依赖的存在！
    2a、越常用，即：被众多模块依赖的模块，一定要极度高内聚！

3、！！！为了避免函数之间的循环依赖问题，上面定义的函数不允许使用下面定义的函数！（即使是插入表中！）

4、！！！如果真要循环依赖的话，不要在 main chunk 直接 require，在函数中使用是没有问题的，
    除非那个模块出现了循环依赖问题，不然应该是没有问题的。